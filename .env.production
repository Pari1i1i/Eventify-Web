# API langsung ke VPS (permanen, bukan tunnel sementara).
# Dipakai oleh `npm run build` -> bundle memanggil API eksternal langsung (CORS sudah "*").
VITE_API_BASE_URL=http://139.190.96.203:8093/api/v1